# models.py patch

Target: `crime_pipeline/models.py`, inside `ExtractedArticleData`, after `# Incident` at current line 252 and before `weapon_type`.

```diff
@@
     # Incident
+    incident_type: Optional[
+        Literal[
+            "homicide",
+            "attempted_homicide",
+            "accident",
+            "traffic_accident",
+            "suicide",
+            "historical_retrospective",
+            "arrest_only",
+            "protest",
+            "other",
+            "unknown",
+        ]
+    ] = None
     weapon_type: Optional[
         Literal["firearm", "knife", "blunt", "explosive", "vehicle", "other", "unknown"]
     ] = None
```

Rationale: optional/default `None` keeps older extracted JSON valid. The field is article-level incident classification, so it belongs before weapon/lethality fields.
