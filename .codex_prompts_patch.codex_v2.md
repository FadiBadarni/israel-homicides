# prompts.py patch

Target 1: `crime_pipeline/extraction/prompts.py`, insert before `VICTIM OUTCOME RULE:` at current line 35.

```text
INCIDENT TYPE RULE:
- Set incident_type to classify the article's PRIMARY CURRENT incident, not every historical fact mentioned in the article.
- "homicide": a current article about a person killed by another person or suspected criminal violence (נרצח, נורה למוות, נהרג מירי, قُتل, لقي مصرعه بإطلاق نار, جريمة قتل).
- "attempted_homicide": a current article about intentional violence that could have killed someone, but no death is confirmed (ניסיון רצח, ירי לעבר, דקירה קשה, محاولة قتل, إطلاق نار على, طعن), including critical-condition cases.
- "accident": non-traffic accidental death or injury, including workplace, tractor, fall, machinery, drowning, fire, or construction accidents when no intentional attack is described.
- "traffic_accident": road crash / collision / run-over incident unless the article explicitly describes intentional vehicular homicide.
- "suicide": self-inflicted death or suspected suicide.
- "historical_retrospective": anniversary, memorial, commemoration, statistics, or background article where deaths happened in the past and the article is not reporting a new/current homicide incident (e.g. Land Day deaths from 1976).
- "arrest_only": article mainly about an arrest, fraud, cheating, weapons possession, police operation, or court action with no described homicide/attempted-homicide incident.
- "protest": demonstration, commemoration, strike, or public disorder article with no current homicide/attempted-homicide incident.
- "other": clearly none of the above.
- "unknown": insufficient article text to classify.
When incident_type is not "homicide" or "attempted_homicide", still extract factual fields that are explicitly present, but do not force victim_outcome="died" from historical, accidental, traffic, suicide, protest, or arrest-only context.
```

Target 2: JSON schema block, insert at the start of the incident fields, before `"weapon_type"` around current line 69.

```diff
@@
  "incident_type": "homicide" | "attempted_homicide" | "accident" | "traffic_accident" | "suicide" | "historical_retrospective" | "arrest_only" | "protest" | "other" | "unknown" | null,
  "weapon_type": "firearm" | "knife" | "blunt" | "explosive" | "vehicle" | "other" | "unknown" | null,
  "weapon_subtype": string | null,                        // e.g. "handgun", "automatic firearm"
  "num_victims": integer,
 
  "suspect_name": string | null,
```
