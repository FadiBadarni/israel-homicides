# relevance.py patch

Target: `crime_pipeline/extraction/relevance.py`.

```diff
@@
 _PLACEHOLDER_STRINGS = frozenset({
@@
 })
+
+_KEEP_INCIDENT_TYPES = frozenset({"homicide", "attempted_homicide"})
+
+_DROP_INCIDENT_TYPES = frozenset({
+    "accident",
+    "traffic_accident",
+    "suicide",
+    "historical_retrospective",
+    "arrest_only",
+    "protest",
+    "other",
+    "unknown",
+})
@@
     if not isinstance(extraction, Mapping):
         return False, "no_extraction_data"
+
+    incident_type_raw = extraction.get("incident_type")
+    incident_type = (
+        incident_type_raw.strip().lower()
+        if isinstance(incident_type_raw, str)
+        else incident_type_raw
+    )
+    if incident_type in _DROP_INCIDENT_TYPES:
+        return False, f"incident_type_{incident_type}"
+    # Null/missing keeps legacy behavior for already-extracted rows. Unknown
+    # strings also fall through instead of creating a new false-negative mode.
+    if incident_type in _KEEP_INCIDENT_TYPES:
+        return True, "kept"
 
     has_victim = any(
```

Also update the docstring drop/keep rules around `is_homicide_extraction` to mention:

```text
Drop explicit non-homicide incident_type values before legacy signal checks.
Keep explicit incident_type values "homicide" and "attempted_homicide".
If incident_type is null/missing, fall back to the older signal-based behavior for backward compatibility.
```

This deliberately keeps `attempted_homicide` even when `victim_outcome="survived"`; if the article is explicitly tagged as attempted homicide, it remains a follow-up seed. If you want `victim_outcome="survived"` to override it, move the `outcome == "survived"` check above the keep rule.
